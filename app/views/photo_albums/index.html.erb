<h1>Albums</h1>

<table>
  <tr>
    <th>Title</th>
    <th>User</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @photo_albums.each do |photo_album| %>
  <tr>
    <td><%= link_to photo_album.title, photo_album %></td>
    <td><%= link_to photo_album.user.name, photo_album.user %></td>
    <td><%= link_to 'Edit', edit_photo_album_path(photo_album) %></td>
    <td><%= link_to 'Destroy', photo_album, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Photo album', new_photo_album_path %> | 
<%= link_to 'Users', users_path %> | 
<%= link_to 'Photos', photos_path %>
